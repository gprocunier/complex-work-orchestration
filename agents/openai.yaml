interface:
  display_name: "Complex Work Orchestration"
  short_description: "Policy-routed MoE harness for large work"
  default_prompt: "Use $complex-work-orchestration to classify this complex project, scaffold Beads lanes, and route architect, project-manager, workerbee, distinguished-SME review, local-worker, and outside-contractor work through the policy control plane. Outside contracts require a share boundary, contractor-only labels, one job-description label, a gated packet, evaluated return, and architect adjudication."
